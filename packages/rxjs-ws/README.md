# rxjs-ws

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rxjs-ws` to build the library.

## Running unit tests

Run `nx test rxjs-ws` to execute the unit tests via [Jest](https://jestjs.io).
